/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ["DM Sans", "sans-serif"], // Overrides Tailwind's default sans-serif
      },
    },
  },
  plugins: [],
};
